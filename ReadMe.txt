1.Install node and gulp on your machine.
2.Go to solution directory in console and run 'npm install' - it should install all needed dependencies.
3.Make sure port 9009 is free to use.
4.Run gulp from the same directory - it will create src\app.js file
  and info-box should be opened in your default browser.

